 function square(number) {
  return number * number;
}

 function sum(number) {
    return number * number;
  }

   function divide(number) {
    divided()
    return number * number;
  }

  export default function divided(number) {
    return number * number;
  }
