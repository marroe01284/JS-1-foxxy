// Understanding Functions Basics - Parameters vs. Arguments
// Task Description
// Create a JavaScript program that demonstrates the basics of functions, focusing on parameters vs. arguments, defining functions, and invoking (calling) them. Follow these steps to complete the task:

// Defining Functions:

// Define a function named calculateArea that takes two parameters: length and width. The function should calculate the area of a rectangle and return it.
// Invoking Functions:

// Invoke the calculateArea function with two arguments: 10 (for length) and 5 (for width), and store the result in a variable. Log this variable to the console to display the area of the rectangle.
// Understanding Parameters vs. Arguments:

// Define another function named greetUser that takes one parameter: name. The function should log a greeting to the console, incorporating the name provided.
// Invoke greetUser with one argument: your name or a sample name, to see the greeting in the console.
// Optional Challenge - Default Parameters:

// Modify the greetUser function to have a default parameter for name. If no argument is passed when the function is called, it should greet an anonymous user.